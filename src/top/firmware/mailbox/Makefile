# Makefile for RISC-V mailbox firmware
# 使用通用 Makefile 模板

# # 编译选项扩展
# CFLAGS += -DPRINTF_DISABLE_SUPPORT_EXPONENTIAL
# CFLAGS += -DPRINTF_DISABLE_SUPPORT_FLOAT
# CFLAGS += -DPRINTF_DISABLE_SUPPORT_LONG_LONG
# CFLAGS += -DPRINTF_DISABLE_SUPPORT_PTRDIFF_T

# 包含通用 Makefile
include ../generic.mk
